# verihook Fastify Example

This example demonstrates how to verify webhooks in **Fastify** using `verihook`.

## Running Locally

```bash
cd examples/fastify-server
npm install
npm run dev
```

Server runs on `http://localhost:3000`.
